# Environment Variables Template for Google Scholar Research Agent
# Copy this content to a .env file and fill in your actual API keys

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize OpenAI settings
# OPENAI_MODEL=gpt-4
# OPENAI_MAX_TOKENS=4000
# OPENAI_TEMPERATURE=0.3

# Google API Configuration (Service Account)
# Instead of using a JSON file, you can set these environment variables
# Get these values from your Google Cloud service account JSON file

GOOGLE_TYPE=service_account
GOOGLE_PROJECT_ID=your_project_id_here
GOOGLE_PRIVATE_KEY_ID=your_private_key_id_here
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_content_here\n-----END PRIVATE KEY-----\n"
GOOGLE_CLIENT_EMAIL=your_service_account_email_here
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
GOOGLE_TOKEN_URI=https://oauth2.googleapis.com/token
GOOGLE_AUTH_PROVIDER_X509_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
GOOGLE_CLIENT_X509_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your_service_account_email_encoded_here
GOOGLE_UNIVERSE_DOMAIN=googleapis.com

# Legacy Google API Configuration (File-based - Optional fallback)
# If you prefer to use JSON files instead of environment variables, set these:
# GOOGLE_CREDENTIALS_FILE=credentials.json
# GOOGLE_SERVICE_ACCOUNT_PATH=path/to/your/service-account.json

# CAPTCHA Solving Service Configuration (Enhanced Feature)
# For automatic CAPTCHA solving when Google Scholar blocks access
# Sign up at https://2captcha.com to get your API key
# This is OPTIONAL but highly recommended for reliable Google Scholar access
TWOCAPTCHA_API_KEY=your_2captcha_api_key_here

# Alternative CAPTCHA services (choose one):
# ANTICAPTCHA_API_KEY=your_anticaptcha_api_key_here
# CAPSOLVER_API_KEY=your_capsolver_api_key_here

# Research Agent Configuration
# Optional: Customize research parameters
# MAX_PAPERS_PER_QUERY=10
# RELEVANCE_THRESHOLD=0.7
# QUALITY_THRESHOLD=0.6

# Instructions:
# 1. Create a file named ".env" in the same directory as the Python scripts
# 2. Copy the environment variables above and replace placeholder values with your actual credentials
# 3. For Google API: Either use environment variables (recommended) OR place JSON file as described
# 4. Save the file
# 5. The Python scripts will automatically load the configuration from this file

# Note: Environment variables take precedence over JSON files for Google authentication 